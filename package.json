{
  "name": "minimalist-web-notepad",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "A minimalist web notepad rewritten for Cloudflare Workers and KV.",
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "npm test && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.49.0"
  }
}
